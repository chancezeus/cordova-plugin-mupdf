repositories {
    jcenter()
    flatDir {
        dirs 'libs'
    }
}

dependencies {
    compile(name: 'mupdf-plugin', ext: 'aar')
}

android {
    defaultConfig {
        minSdkVersion 16
    }

    packagingOptions {
        exclude 'META-INF/NOTICE'
        exclude 'META-INF/LICENSE'
    }
}